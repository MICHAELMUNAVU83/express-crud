You have t0 create the database with

```

npm i express pg cors joi  body-parser nodemon

```

Then create the database with the following command in your terminal:

```

psql -U postgres
CREATE DATABASE express_crud_dev;


```
